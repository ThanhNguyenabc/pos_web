export interface Specification {
  id: string;
  businessSize: string;
  productId: string;
  posType: string;
  softwareType: string;
  freeTrial: string;
  merchantService: string;
  pricingModel: string;
  priceRange: string;
}
